import React, { useState } from 'react';
import './ProductContainer.styles.css';
import { SaveFavorite } from '../saveFavorite/SaveFavorite.component';
import { ProductImage } from '../productImage/ProductImage.component';
import { ProductTitleText } from '../productDetail/ProductTitleText.component';
import '../productDetail/ProductTitleText.styles.css';
import { Button } from '../Button/Button.component';
import Counter from '../Counter/Counter';
import { OptionsPack } from '../optionsPack/OptionsPack.component';

export const ProductContainer = () => {
  const [count, setCount] = useState(1);
  const [select, setSelect] = useState('');
  const [selectedIcon, setSelectedIcon] = useState(false);

  const options = [
    {
      id: 1,
      weight: '250g glass jar',
      price: 'DKK 350',
    },
    {
      id: 2,
      weight: '100g flat pack',
      price: 'DKK 100',
    },
  ];
  return (
    <div className="outer-container">
      <div className="product-container">
        <div className="favorite-icon">
          <SaveFavorite
            selectedIcon={selectedIcon}
            setSelectedIcon={setSelectedIcon}
          />
        </div>
        <div className="product-detail">
          <div className="product-image">
            <ProductImage className="product-image" />
          </div>
          <div className="product-detail-right">
            <ProductTitleText
              heading="Juniper Berries"
              info="this is autogenerated dskkdsl from main"
            />
            <div className="radio-button">
              <OptionsPack
                select={select}
                setSelect={setSelect}
                options={options}
              />
            </div>
            <div className="counter-cart-button">
              <div className="counter-button">
                <Counter count={count} setCount={setCount} />
              </div>
              <Button label="ADD TO CART " type="addToCart" />
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};
